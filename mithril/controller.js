var fiveStar = fiveStar || {};

fiveStar.controller = {
  this.widget = fiveStar.rating;
  m.request({
    method : "POST",
    url: "127.0.0.1/ready"
  });

};

//better to put functionality in the view-model?